// use this section to define variable that will be globally used across all other scripts.
var timeRefresh = function(){ window.setInterval(main.grabMessages, 15000);
  console.log("I am working")
};
